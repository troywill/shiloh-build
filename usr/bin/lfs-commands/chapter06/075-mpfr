#!/bin/bash
set +h
set -e

cd $PKGDIR
./configure --prefix=/usr --enable-thread-safe \
  --docdir=/usr/share/doc/mpfr-3.0.0
make
make install
make html
make install-html
echo -e "\n\nTotalseconds: $SECONDS\n"
exit
